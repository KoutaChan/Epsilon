/**
 * KL・LR制御、選択行動の方策勾配学習連続学習、事前学習、勾配更新、継続学習チェックポイントを提供する。
 *
 * <p>学習器はモデル・AdamW・制御状態を一組として保存・復元する。Trainerと事前学習erが共有する
 * DataParallel、入力利用権、KL観測、学習サイクル採否の内部契約はこのパッケージに閉じる。
 * 対局評価の実行は対戦評価、推論とGPU資源は実行時、永続サンプルの読み出しはデータへ委譲する。
 */
package com.epsilon.pico.ai.decision.training;
