/**
 * Decision の仮説検証用検証用の予測器を実装する。
 *
 * <p>検証用の予測器はチェックポイントを更新せず、限定した信号や表現の有無を測る。CLI 引数の解析と終了状態の変換は {@code
 * com.epsilon.nano.workflow.probe} が担当する。
 */
package com.epsilon.nano.ai.decision.probe;
